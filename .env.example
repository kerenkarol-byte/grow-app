# Copy this file to .env.local and fill in your values.
# .env.local is gitignored by Vite by default — never commit real keys.

# YouTube Data API v3 key
# Get yours at: https://console.cloud.google.com → APIs & Services → Credentials
# Enable the "YouTube Data API v3" for your project first.
VITE_YOUTUBE_API_KEY=your_key_here

# ── Future integrations ───────────────────────────────────────────────────────
# Add new API keys here as additional sources are integrated.
# See the "Future podcast source" and "Events / Workshops / Retreats" sections
# in src/App.jsx for the expected hook shapes.
